# アンドロイドの家計簿アプリを作る。(KakeiboGX)

# 基本 (Jetpack Compose版)
下に4つ項目がある (Bottom Navigation)
1.入力 (InputScreen)
2.カレンダー (CalendarScreen)
3.レポート (ReportScreen / CategoryReportScreen)
4.その他 (SettingsScreen / SubSettingsScreens)


# 入力UI
1. 支出、収入の2タブがある。

2. その下に日付がある。左右に矢印ボタンがあって、翌日前日に移動でき、カレンダーアイコンをクリックするとカレンダーで指定の日付にできる。
    - HHHH/MM/DD/(曜日)形式。

3. その下にメモ。要するに何に使ったか。タップするとキーボードが開く。

4. その下に金額入力。タップするとボトムシートの電卓UIが開く。

    - 1~4までは画面に固定されているUI (Edge-to-Edge対応)。

5. その下にカテゴリーがある。正方形の中にアイコンがあって、支出に対して1つ選ぶ。
    - カテゴリーは設定画面で編集可能。カラーやアイコンの指定対応。

6. 下に支出を入力する「保存」ボタンがあり、タップすると入力した内容を登録する。

7. 収入は1~5と同様。

## 詳細UI
1~4までの行で上から40%ぐらい、5が60％ぐらい。Jetpack ComposeのModifier.weight等で柔軟なレイアウトを実現。


# カレンダーUI
1. １行目は今月が出る(YYYY/MM)。左右に矢印ボタンがあって、前月翌月移動できる。
2. その下にカレンダーが出る。各セルに収入、支出がそれぞれ赤字青字で入る。収入がセルの上段、支出がセルの下段。(kなどの省略は廃止し、フル金額表示に変更)
3. カレンダーの下に月間の収入、支出、合計(収支)が出る。
4. その下に日付け、その日の収支のリストがスクロール可能な行で出る。
    - リストの項目をタップするとボトムシート(DailyDataEditSheet)が開き、編集画面に移動する。
    - 編集画面では「保存」「削除」「コピーして新規作成」のボタンがある。


# レポートUI
1. １行目はカレンダーUIと同様の月変更UI。
2. その下に2つタブがあり、支出、収入を切り替え可能。
3. カテゴリごとの割合を表示する円グラフ（MPAndroidChartを利用し、AndroidViewでラップ）。
    - グラフ下部に、今月のカード利用額（来月支払い分）を表示。
4. その下にカテゴリごとのリストが表示される。
    - 金額だけでなく、予算（クォータ）が設定されている場合は、残額・日割りでの今日の目標額・今日の使用額などを表示。
    - リストの項目をタップするとカテゴリ別の月間レポートに遷移。
    - カテゴリ別の月間レポートは棒グラフで月別推移を確認可能。


# その他（設定UI）
1. Google Drive クラウド同期
    - Googleログインを行い、内部のSQLiteデータをJSON形式に変換してGoogle DriveのAppFolderに保存・同期する。
2. カテゴリ編集
    - カスタムカラーを選択可能。
3. 予算（クォータ）設定
    - インライン入力形式で、全カテゴリの予算を一覧から直接入力して一括保存可能。
4. 固定収支設定
    - 毎月決まった日に自動追加したいデータの設定。
5. CSV エクスポート/インポート
    - 外部連携用の汎用CSVフォーマット対応。

# 要望事項(対応済み)
1. アプリ開いたまま翌日とかになると日付が前のまま固定される問題 -> LaunchedEffectや状態管理で再取得。
2. カード、現金で払った結果がUIで見れない -> 決済方法を保存し、レポート画面で「来月のカード払い」として合計表示。
3. レポート画面に予算UIを常時表示したい -> 支出が0円でも予算設定があればリストに表示されるよう改善。
4. 携帯変えたらUIの上の方が被る -> Jetpack Composeの `WindowInsets.safeDrawing` でEdge-to-Edgeに完全対応し余白を確保。
5. 固定収支は絶対変わらないから最初から表示しててもいい -> 事前設定UIとして実装済み。